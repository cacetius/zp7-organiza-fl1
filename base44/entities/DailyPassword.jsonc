{
  "name": "DailyPassword",
  "type": "object",
  "properties": {
    "solicitante": {
      "type": "string"
    },
    "solicitante_email": {
      "type": "string"
    },
    "aprovador": {
      "type": "string"
    },
    "motivo": {
      "type": "string"
    },
    "data": {
      "type": "string",
      "format": "date"
    },
    "status": {
      "type": "string",
      "enum": [
        "pendente",
        "aprovada",
        "rejeitada"
      ],
      "default": "pendente"
    },
    "turno": {
      "type": "string",
      "enum": [
        "primeiro",
        "segundo",
        "terceiro"
      ]
    }
  },
  "required": [
    "solicitante",
    "motivo"
  ],
  "rls": {
    "create": null,
    "read": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "data.solicitante_email": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "created_by_id": "{{user.id}}",
          "data.status": "pendente"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by_id": "{{user.id}}",
          "data.status": "pendente"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}