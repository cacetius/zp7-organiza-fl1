{
  "name": "MaintenanceRequest",
  "type": "object",
  "properties": {
    "testor_nome": {
      "type": "string",
      "description": "Testor relacionado"
    },
    "tipo_falha": {
      "type": "string",
      "enum": [
        "mecanica",
        "eletrica",
        "software",
        "calibracao",
        "outro"
      ]
    },
    "descricao": {
      "type": "string"
    },
    "prioridade": {
      "type": "string",
      "enum": [
        "baixa",
        "media",
        "alta",
        "critica"
      ],
      "default": "media"
    },
    "status": {
      "type": "string",
      "enum": [
        "aberto",
        "em_andamento",
        "concluido"
      ],
      "default": "aberto"
    },
    "tempo_estimado_reparo": {
      "type": "number",
      "description": "Em minutos"
    },
    "pecas_necessarias": {
      "type": "string"
    },
    "impacto_carros": {
      "type": "number",
      "description": "Carros perdidos estimados"
    },
    "responsavel": {
      "type": "string"
    },
    "turno": {
      "type": "string",
      "enum": [
        "primeiro",
        "segundo",
        "terceiro"
      ]
    },
    "observacao": {
      "type": "string"
    }
  },
  "required": [
    "testor_nome",
    "tipo_falha",
    "descricao"
  ],
  "rls": {
    "create": {
      "created_by": "{{user.email}}"
    },
    "read": {
      "$or": [
        {
          "created_by": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "role": "manutencao"
          }
        },
        {
          "user_condition": {
            "role": "lider"
          }
        },
        {
          "data.turno": "{{user.data.turno}}"
        }
      ]
    },
    "update": {
      "$or": [
        {
          "created_by": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "role": "manutencao"
          }
        },
        {
          "user_condition": {
            "role": "lider"
          }
        },
        {
          "data.responsavel": "{{user.full_name}}"
        },
        {
          "data.turno": "{{user.data.turno}}"
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "role": "lider"
          }
        }
      ]
    }
  }
}